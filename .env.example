# DeepEye Lite — docker-compose 용 환경변수 (예시 스켈레톤)
# 실제 사용 시 이 파일을 `.env` 로 복사한 뒤 값을 채운다.
#   cp .env.example .env

# 호스트 외부에 노출되는 포트
BACKEND_PORT={백엔드포트}
FRONTEND_PORT={프론트엔드포트}
MEDIAMTX_API_PORT={MediaMTX_API_포트}
MEDIAMTX_HLS_PORT={MediaMTX_HLS_포트}
MEDIAMTX_RTSP_PORT={MediaMTX_RTSP_포트}
